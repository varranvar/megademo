[project]
name = "lp-bug-triage"
version = "0.1.0"
description = "Launchpad bug triage MCP server for opencode"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "launchpadlib>=2.0.0",
    "requests>=2.28.0",
]

[project.scripts]
lp-bug-triage = "server:main"
