database:
  path: data/mdmo.db

ubuntu:
  releases:
    - jammy
    - noble

scan:
  intervals:
    launchpad: 86400
    nvd: 21600
    oval: 86400

scoring:
  weights:
    bugs: 0.25
    cve: 0.35
    freshness: 0.20
    plugins: 0.20

nvd:
  api_key: ${NVD_API_KEY}

launchpad:
  api_base_url: https://api.launchpad.net/devel

oval:
  base_url: https://security-metadata.canonical.com/oval
